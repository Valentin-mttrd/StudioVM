---
interface Props {
  class?: string;
  as?: string;
}

const { class: className = '', as = 'div' } = Astro.props;
const Tag = as as keyof HTMLElementTagNameMap;
---
<Tag class:list={['mx-auto w-full max-w-[90rem] px-6 sm:px-8 lg:px-12', className]}>
  <slot />
</Tag>
